<Page x:Class="AlexAssistant.ModelTrainingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:AlexAssistant"
      mc:Ignorable="d"
      d:DesignHeight="480" d:DesignWidth="400"
      Title="ModelTrainingPage"
      Background="#FF2D2D30"
      Foreground="WhiteSmoke"
      Loaded="Page_Loaded" Unloaded="Page_Unloaded">

    <Page.Resources>
        <Style TargetType="Button" x:Key="StartButtonStyle">
            <Setter Property="Background" Value="#FF007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF005A9E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#FF555555"/>
                                <Setter Property="Foreground" Value="#FFAAAAAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ProgressBar" x:Key="ProgressBarStyle">
            <Setter Property="Height" Value="10"/>
            <Setter Property="Background" Value="#FF444444"/>
            <Setter Property="Foreground" Value="#FF007ACC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid Name="TemplateRoot">
                            <Border Name="PART_Track" CornerRadius="5" Background="{TemplateBinding Background}" />
                            <Border Name="PART_Indicator" CornerRadius="5" Background="{TemplateBinding Foreground}"
                                    HorizontalAlignment="Left" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,5">
            <TextBlock x:Name="StatusTextBlock" Text="Loading camera..."
                       FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <TextBlock x:Name="TimerTextBlock" Text="30 sec" Visibility="Collapsed"
                       Foreground="#FF87CEEB" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Margin="15,0,0,0"/>
        </StackPanel>

        <TextBlock x:Name="InstructionTextBlock" Grid.Row="1"
                   Text="Please follow the instructions."
                   FontSize="12" Foreground="LightGray" HorizontalAlignment="Center"
                   TextWrapping="Wrap" TextAlignment="Center" Margin="5,0,5,10" Visibility="Collapsed"/>

        <Border Grid.Row="2"
                Background="#FF404040"
                CornerRadius="8"
                Margin="15,0,15,10">
            <Grid ClipToBounds="True">
                <Image x:Name="VideoImage" Stretch="Uniform">
                    <Image.OpacityMask>
                        <DrawingBrush Stretch="Uniform">
                            <DrawingBrush.Drawing>
                                <GeometryDrawing Brush="White">
                                    <GeometryDrawing.Geometry>
                                        <EllipseGeometry Center="50,50" RadiusX="35" RadiusY="48"/>
                                    </GeometryDrawing.Geometry>
                                </GeometryDrawing>
                            </DrawingBrush.Drawing>
                        </DrawingBrush>
                    </Image.OpacityMask>
                </Image>
            </Grid>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Vertical" HorizontalAlignment="Center" Margin="0,5,0,5">
            <Button x:Name="StartButton" Content="Start Recording"
                    Width="160"
                    Style="{StaticResource StartButtonStyle}"
                    Click="StartButton_Click" IsEnabled="False"/>

            <ProgressBar x:Name="CaptureProgress"
                         Width="250" Margin="0,10,0,0"
                         Visibility="Collapsed" Minimum="0" Maximum="30"
                         Style="{StaticResource ProgressBarStyle}"/>
        </StackPanel>
    </Grid>
</Page>